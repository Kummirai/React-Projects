import React from 'react';
import Header from "./Header.jsx";

const Home = ({navLinks}) => {
    return (
        <>
        <Header navLinks={navLinks} />
        </>
    );
};

export default Home;